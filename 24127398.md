# Sub-problem: Saving Data System
**Branch Git:** `feature/24127398`  
**Người phụ trách:** Nguyễn Nhựt Huy  
**Thuộc project:** *Food Recommend System (Gemini + Map API)*  

---

## I. Mục tiêu
Thiết kế và triển khai **hệ thống lưu trữ dữ liệu hội thoại** của người dùng trong ứng dụng gợi ý món ăn.  

Yêu cầu chính:
- Lưu lịch sử trò chuyện giữa người dùng và chatbot (Gemini API).  
- Đảm bảo **ẩn danh** và **bảo mật** thông tin người dùng.  
- Cho phép người dùng xem lại lịch sử chat.  
- Dễ tích hợp với các module khác (frontend, Gemini API, Map API).

---

## II. Phân tích bài toán con

| Thành phần | Mô tả | Đầu ra mong muốn |
|-------------|-------|------------------|
| **1. Lưu hội thoại (conversation)** | Nhận dữ liệu từ backend khi người dùng gửi/nhận tin nhắn, lưu toàn bộ nội dung vào cơ sở dữ liệu. | Cấu trúc dữ liệu dạng JSON chứa các message. |
| **2. Ẩn danh hóa người dùng** | Sinh mã định danh ẩn danh (`anon_user_id` hoặc `token`) thay cho tài khoản thực. | Token định danh duy nhất, không chứa PII. |
| **3️. Bảo mật dữ liệu** | Mã hóa nội dung chat trước khi lưu. Không lưu IP/email. | Dữ liệu được mã hóa trong DB. |
| **4️. Truy xuất lịch sử** | Cho phép lấy danh sách cuộc hội thoại theo mã ẩn danh. | API `/get_history` trả JSON. |
| **5️. Xóa và quản lý dữ liệu** | Thêm tính năng xóa toàn bộ lịch sử ẩn danh (theo token). | API `/delete_history` hoạt động đúng. |

---

## III. Đề xuất công nghệ

| Thành phần | Công cụ/Thư viện | Ghi chú |
|-------------|------------------|---------|
| **Backend** | Node.js + Express | Xử lý API lưu / truy xuất dữ liệu |
| **Database** | MongoDB Atlas (cloud) | Lưu JSON hội thoại, linh hoạt, dễ triển khai |
| **Bảo mật** | AES-256 hoặc thư viện `crypto` trong Node.js | Mã hóa nội dung chat |
| **Ẩn danh hóa** | UUID + Token lưu cục bộ (localStorage) | Người dùng không cần đăng nhập |
| **Tích hợp API** | Gọi Gemini API để lấy phản hồi và lưu vào DB cùng user message | Kết nối trực tiếp với module Gemini |
| **Map API** | Không lưu ảnh bản đồ, chỉ lưu toạ độ hoặc địa chỉ text | Giảm dung lượng và tránh lộ vị trí |

---

## IV. Cấu trúc dữ liệu đề xuất

```json
{
  "anon_user_id": "uuid-v4",
  "conversations": [
    {
      "conversation_id": "uuid-v4",
      "created_at": "2025-11-04T10:30:00Z",
      "messages": [
        { "role": "user", "content": "Tôi muốn ăn phở bò ở quận 3" },
        { "role": "assistant", "content": "Gợi ý: Phở Phú Vương - Nguyễn Trãi, Quận 3" }
      ],
      "metadata": {
        "location": "Quận 3, TP.HCM",
        "map_link": "https://maps.google.com/?q=Phở Phú Vương"
      }
    }
  ]
}

```
## V. So sánh các lựa chọn công nghệ

| Tiêu chí | PostgreSQL | MongoDB | Firebase |
|-----------|-------------|----------|-----------|
| **Kiểu dữ liệu** | Cứng (bảng/quan hệ) | Linh hoạt (document JSON) | Linh hoạt + realtime |
| **Dễ triển khai** | Trung bình |  Dễ |  Rất dễ |
| **Khả năng lưu hội thoại** | Tốt nhưng cần join |  Tốt |  Tốt |
| **Khả năng mở rộng** | Cao | Cao | Trung bình |
| **Phù hợp với dự án** |  Không cần thiết |  Rất phù hợp |  Tốt nếu dùng frontend nhiều |

 >**Kết luận:** Chọn **MongoDB Atlas** để lưu hội thoại vì cấu trúc linh hoạt, dễ tích hợp với API và lưu dữ liệu JSON trực tiếp.
